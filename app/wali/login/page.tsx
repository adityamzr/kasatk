'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';import{LoaderCircle,ArrowRight}from'lucide-react';
export default function ParentLogin(){const [phone,setPhone]=useState('');const [dateOfBirth,setDate]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage('');try{const r=await fetch('/api/auth/parent-login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({phone,dateOfBirth})});const d=await r.json();if(r.ok) window.location.href='/wali';else setMessage(d.message||'Login gagal.')}catch{setMessage('Tidak dapat terhubung ke server. Silakan coba lagi.')}finally{setLoading(false)}}return <main className="auth-shell parent-auth"><div className="auth-card"><div className="auth-brand"><img src="/logo-paud.jpg"/><div><b>Cerdas Ceria</b><span>Portal Orang Tua</span></div></div><p className="eyebrow">BUKU DIGITAL ANAK</p><h1>Selamat datang, Ayah Bunda</h1><p className="auth-muted">Masuk untuk melihat status SPP dan saldo tabungan anak.</p><form onSubmit={submit}><label>Nomor WhatsApp terdaftar<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Contoh: 08123456789" inputMode="tel"/></label><label>Tanggal lahir anak<input type="date" value={dateOfBirth} onChange={e=>setDate(e.target.value)}/></label>{message&&<div className="error">{message}</div>}<button className="primary auth-submit" disabled={loading} aria-busy={loading}>{loading?<><LoaderCircle className="loading-spinner" size={16}/> Memeriksa...</>:'Lihat buku digital'}</button></form><Link className="parent-link" href="/login">Masuk sebagai admin sekolah <ArrowRight size={15} aria-hidden="true"/></Link></div></main>}
