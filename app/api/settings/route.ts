export const dynamic='force-dynamic';
import { NextResponse } from 'next/server';import { Prisma } from '@prisma/client';import { prisma } from '@/lib/prisma';import { getCurrentUser } from '@/lib/auth';import { requirePermission,errorResponse } from '@/lib/api';import { settingsSchema } from '@/lib/settings';
function view(setting:any){return {schoolName:setting.schoolName,schoolAddress:setting.schoolAddress??'',schoolPhone:setting.schoolPhone??'',schoolEmail:setting.schoolEmail??'',defaultSppAmount:setting.defaultSppAmount?.toString()??null,receiptPrefix:setting.receiptPrefix,timezone:setting.timezone}}
export async function GET(){const denied=await requirePermission('users.manage');if(denied)return denied;try{const setting=await prisma.schoolSetting.findUnique({where:{id:1}});return NextResponse.json(view(setting??{schoolName:'',schoolAddress:'',schoolPhone:'',schoolEmail:'',defaultSppAmount:null,receiptPrefix:'KWT',timezone:'Asia/Jakarta'}))}catch(e){return errorResponse(e)}}
export async function PATCH(req:Request){const denied=await requirePermission('users.manage');if(denied)return denied;try{const user=await getCurrentUser();if(!user)return NextResponse.json({message:'Sesi login tidak ditemukan.'},{status:401});const input=settingsSchema.parse(await req.json());const before=await prisma.schoolSetting.findUnique({where:{id:1}});const setting=await prisma.schoolSetting.upsert({where:{id:1},create:{id:1,schoolName:input.schoolName,schoolAddress:input.schoolAddress||null,schoolPhone:input.schoolPhone||null,schoolEmail:input.schoolEmail||null,defaultSppAmount:input.defaultSppAmount===null?null:new Prisma.Decimal(input.defaultSppAmount),receiptPrefix:input.receiptPrefix,timezone:input.timezone},update:{schoolName:input.schoolName,schoolAddress:input.schoolAddress||null,schoolPhone:input.schoolPhone||null,schoolEmail:input.schoolEmail||null,defaultSppAmount:input.defaultSppAmount===null?null:new Prisma.Decimal(input.defaultSppAmount),receiptPrefix:input.receiptPrefix,timezone:input.timezone}});await prisma.auditLog.create({data:{userId:user.id,action:'UPDATE_SCHOOL_SETTINGS',entity:'SchoolSetting',entityId:'1',beforeData:before?view(before):Prisma.JsonNull,afterData:view(setting)}});return NextResponse.json({success:true,settings:view(setting)})}catch(e){return errorResponse(e)}}
