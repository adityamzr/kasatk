export const dynamic='force-dynamic';
import{NextResponse}from'next/server';import{prisma}from'@/lib/prisma';import{requirePermission,errorResponse}from'@/lib/api';
export async function GET(_req:Request,{params}:{params:{studentId:string}}){const d=await requirePermission('savings.manage');if(d)return d;try{const student=await prisma.student.findUnique({where:{id:params.studentId},include:{classRoom:true,savings:{orderBy:{transactionDate:'desc'},include:{recordedBy:{select:{name:true}},receipt:true}}}});if(!student)return NextResponse.json({message:'Siswa tidak ditemukan.'},{status:404});const latest=student.savings.find(x=>x.status==='COMPLETED');return NextResponse.json({student:{id:student.id,name:student.name,nis:student.nis,classRoom:student.classRoom,status:student.status},balance:latest?.balanceAfter.toString()??'0',transactions:student.savings.map(x=>({...x,amount:x.amount.toString(),balanceAfter:x.balanceAfter.toString()}))})}catch(e){return errorResponse(e)}}
