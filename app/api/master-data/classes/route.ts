export const dynamic='force-dynamic';
import {NextResponse} from 'next/server';import{prisma}from'@/lib/prisma';import{requirePermission,errorResponse}from'@/lib/api';import{getCurrentUser}from'@/lib/auth';
export async function GET(){const d=await requirePermission('students.manage');if(d)return d;try{return NextResponse.json(await prisma.classRoom.findMany({orderBy:[{isActive:'desc'},{name:'asc'}],include:{_count:{select:{students:true}}}}))}catch(e){return errorResponse(e)}}
export async function POST(req:Request){const d=await requirePermission('students.manage');if(d)return d;try{const u=await getCurrentUser();const b=await req.json();if(!b.name?.trim())return NextResponse.json({message:'Nama kelas wajib diisi.'},{status:400});const item=await prisma.$transaction(async tx=>{const created=await tx.classRoom.create({data:{name:b.name.trim(),level:b.level?.trim()||null,academicYear:b.academicYear?.trim()||null}});await tx.auditLog.create({data:{userId:u?.id,action:'CLASS_CREATED',entity:'ClassRoom',entityId:created.id,afterData:{name:created.name,academicYear:created.academicYear}}});return created});return NextResponse.json(item,{status:201})}catch(e){return errorResponse(e)}}
