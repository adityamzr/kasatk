'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';import{LoaderCircle,ArrowRight}from'lucide-react';
export default function LoginPage(){ const [identifier,setIdentifier]=useState(''); const [password,setPassword]=useState(''); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({identifier,password})});const d=await r.json();if(r.ok) window.location.href='/'; else setMessage(d.message);setLoading(false)}
 return <main className="auth-shell"><div className="auth-card"><div className="auth-brand"><img src="/logo-paud.jpg"/><div><b>Cerdas Ceria</b><span>PAUD Finance</span></div></div><p className="eyebrow">PORTAL SEKOLAH</p><h1>Masuk ke dashboard</h1><p className="auth-muted">Kelola SPP dan tabungan siswa dengan lebih mudah.</p><form onSubmit={submit}><label>Email atau username<input value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder="Contoh: bendahara" autoComplete="username"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Masukkan password" autoComplete="current-password"/></label>{message&&<div className="error">{message}</div>}<button className="primary auth-submit" disabled={loading}>{loading?'Memeriksa...':'Masuk ke dashboard'}</button></form><Link className="parent-link" href="/wali/login">Masuk sebagai orang tua <ArrowRight size={15} aria-hidden="true"/></Link></div></main> }
