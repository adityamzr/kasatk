import{redirect}from'next/navigation';import{getCurrentUser,getEffectivePermissionKeys}from'@/lib/auth';import{prisma}from'@/lib/prisma';import AdminShell from'@/components/admin-shell';export default async function AdminLayout({children}:{children:React.ReactNode}){const user=await getCurrentUser();if(!user)redirect('/login');const setting=await prisma.schoolSetting.findUnique({where:{id:1},select:{schoolName:true}}),permissions=getEffectivePermissionKeys(user),roles=user.roles.map(x=>x.role.name);return <AdminShell user={{name:user.name,email:user.email,roles}} permissions={permissions} schoolName={setting?.schoolName||'Administrasi Sekolah'}>{children}</AdminShell>}