'use client';
import Link from 'next/link';import{useEffect,useState}from'react';
export default function NewStudent(){const [classes,setClasses]=useState<any[]>([]),[parents,setParents]=useState<any[]>([]),[mode,setMode]=useState('existing'),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{Promise.all([fetch('/api/master-data/classes'),fetch('/api/master-data/parents')]).then(async([a,b])=>{if(a.ok)setClasses(await a.json());if(b.ok)setParents(await b.json())})},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const v=Object.fromEntries(f.entries());const guardian=mode==='existing'?{type:'existing',parentId:v.parentId,relation:v.relation,isPrimary:v.isPrimary==='on'}:{type:'new',relation:v.relation,isPrimary:v.isPrimary==='on',parent:{name:v.parentName,phone:v.parentPhone,email:v.parentEmail}};const r=await fetch('/api/master-data/students',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({student:{nis:v.nis,name:v.name,dateOfBirth:v.dateOfBirth,gender:v.gender,classRoomId:v.classRoomId,customSppAmount:v.customSppAmount},guardians:[guardian]})});const d=await r.json();if(r.ok)location.href=`/admin/students/${d.id}`;else setMessage(d.message||'Data gagal disimpan.');setBusy(false)}return <div className="section-page narrow-page"><div className="page-title"><div><p className="eyebrow">DATA SEKOLAH / SISWA</p><h1>Tambah Siswa</h1><p className="muted">Buat profil siswa dan hubungkan orang tua/wali.</p></div><Link className="back-link" href="/admin/students">← Kembali</Link></div><form className="form-stack" onSubmit={submit}><section className="panel form-section"><h2>Data siswa</h2><div className="form-grid"><label>NIS *<input name="nis" required/></label><label>Nama lengkap *<input name="name" required/></label><label>Tanggal lahir *<input name="dateOfBirth" type="date" required/></label><label>Jenis kelamin<select name="gender"><option value="">Pilih</option><option value="L">Laki-laki</option><option value="P">Perempuan</option></select></label><label>Kelas *<select name="classRoomId" required><option value="">Pilih kelas</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Nominal SPP khusus (opsional)<input name="customSppAmount" type="number" min="0" placeholder="Gunakan default sekolah"/></label></div></section><section className="panel form-section"><h2>Orang tua / wali</h2><div className="choice-row"><button type="button" className={mode==='existing'?'choice active':'choice'} onClick={()=>setMode('existing')}>Pilih yang sudah terdaftar</button><button type="button" className={mode==='new'?'choice active':'choice'} onClick={()=>setMode('new')}>Tambah data baru</button></div>{mode==='existing'?<label>Orang tua/wali *<select name="parentId" required><option value="">Pilih orang tua</option>{parents.map(p=><option value={p.id} key={p.id}>{p.name} · {p.phone}</option>)}</select></label>:<div className="form-grid"><label>Nama lengkap *<input name="parentName" required/></label><label>Nomor WhatsApp *<input name="parentPhone" required placeholder="0812..."/></label><label>Email<input name="parentEmail" type="email"/></label></div>}<div className="form-grid"><label>Hubungan *<select name="relation" defaultValue="AYAH" required><option value="AYAH">Ayah</option><option value="IBU">Ibu</option><option value="WALI">Wali</option><option value="LAINNYA">Lainnya</option></select></label><label className="check-label"><input name="isPrimary" type="checkbox" defaultChecked/> Jadikan kontak utama</label></div></section>{message&&<div className="error">{message}</div>}<div className="form-actions"><Link className="back-link" href="/admin/students">Batal</Link><button className="primary" disabled={busy}>{busy?'Menyimpan...':'Simpan siswa'}</button></div></form></div>}
