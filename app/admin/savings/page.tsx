'use client';
import Link from 'next/link';import{useEffect,useState}from'react';import{Banknote,Plus,ArrowRight}from'lucide-react';
const money=(n:number|string|null|undefined)=>{const value=Number(String(n??'').replace(/[^0-9-]/g,''));return Number.isFinite(value)?new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(value):'Rp 0'};
export default function Savings(){const[rows,setRows]=useState<any[]>([]),[summary,setSummary]=useState<any>({}),[classes,setClasses]=useState<any[]>([]),[q,setQ]=useState(''),[classId,setClassId]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{const t=setTimeout(()=>{setLoading(true);setError('');Promise.all([fetch(`/api/savings?search=${encodeURIComponent(q)}&classRoomId=${classId}`),fetch('/api/master-data/classes')]).then(async([a,b])=>{const d=await a.json();if(!a.ok)throw Error(d.message);setRows(d.items||[]);setSummary(d.summary||{});if(b.ok)setClasses(await b.json())}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},350);return()=>clearTimeout(t)},[q,classId]);return <div className="section-page"><div className="page-title"><div><p className="eyebrow">KEUANGAN SEKOLAH</p><h1>Tabungan Siswa</h1><p className="muted">Kelola saldo, setoran, dan penarikan tabungan siswa.</p></div></div><div className="filter-row"><input placeholder="Cari NIS atau nama siswa..." value={q} onChange={e=>setQ(e.target.value)}/><select value={classId} onChange={e=>setClassId(e.target.value)}><option value="">Semua kelas</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="savings-summary"><div className="spp-card"><p>Total saldo tabungan</p><b>{money(summary.totalBalance||0)}</b></div><div className="spp-card"><p>Setoran bulan ini</p><b>{money(summary.depositsThisMonth||0)}</b></div><div className="spp-card"><p>Penarikan bulan ini</p><b>{money(summary.withdrawalsThisMonth||0)}</b></div><div className="spp-card"><p>Siswa dengan tabungan</p><b>{summary.studentsWithSavings||0}</b></div></div>{error?<div className="error">{error}</div>:loading?<div className="panel empty">Memuat data...</div>:<section className="panel data-table savings-table">{!rows.length?<div className="empty"><Banknote size={28} aria-hidden="true"/><b>Belum ada data tabungan siswa.</b></div>:<><div className="data-row savings-header"><span>NIS</span><span>NAMA SISWA</span><span>KELAS</span><span>SALDO</span><span>TRANSAKSI TERAKHIR</span><span/></div>{rows.map(s=><Link className="data-row savings-row" href={`/admin/savings/${s.id}`} key={s.id}><span>{s.nis}</span><strong>{s.name}</strong><span>{s.classRoom?.name||'-'}</span><b className="savings-balance">{money(Number(s.currentBalance))}</b><span>{s.lastTransactionDate?new Date(s.lastTransactionDate).toLocaleDateString('id-ID'):'Belum ada transaksi'}</span><ArrowRight size={15} aria-hidden="true"/></Link>)}</>}</section>}</div>}
