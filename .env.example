# Neon PostgreSQL connection string
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Application secret for authentication/session encryption
AUTH_SECRET="replace-with-a-long-random-secret"
