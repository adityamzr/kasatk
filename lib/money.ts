export function formatRupiah(value: string|number|null|undefined, fallback='Rp0') { if (value === null || value === undefined || value === '') return fallback; const n = typeof value === 'number' ? value : Number(String(value).replace(/[^0-9-]/g,'')); if (!Number.isFinite(n)) return fallback; const sign = n < 0 ? '-' : ''; return `${sign}Rp${Math.abs(n).toLocaleString('id-ID')}`; }
