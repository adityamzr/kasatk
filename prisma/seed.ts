import{PrismaClient}from'@prisma/client';import bcrypt from'bcryptjs';import{DEFAULT_ROLE_PERMISSIONS,PERMISSION_CATALOG,expandPermissionKeys}from'../lib/permissions';const prisma=new PrismaClient();
async function main(){await prisma.schoolSetting.upsert({where:{id:1},update:{defaultSppAmount:250000},create:{schoolName:'PAUD Cerdas Ceria',receiptPrefix:'SPP',defaultSppAmount:250000}});const permissionMap=new Map<string,string>();for(const d of PERMISSION_CATALOG){const p=await prisma.permission.upsert({where:{key:d.key},update:{description:d.description},create:{key:d.key,description:d.description}});permissionMap.set(d.key,p.id)}const descriptions:Record<string,string>={SUPER_ADMIN:'Akses penuh aplikasi',BENDAHARA:'Mengelola operasional keuangan sekolah',GURU:'Akses operasional guru'};for(const[name,keys]of Object.entries(DEFAULT_ROLE_PERMISSIONS)){const role=await prisma.role.upsert({where:{name},update:{description:descriptions[name]},create:{name,description:descriptions[name]}});for(const key of expandPermissionKeys(keys)){const permissionId=permissionMap.get(key);if(permissionId)await prisma.rolePermission.upsert({where:{roleId_permissionId:{roleId:role.id,permissionId}},update:{},create:{roleId:role.id,permissionId}})}}const adminRole=await prisma.role.findUniqueOrThrow({where:{name:'SUPER_ADMIN'}}),adminPassword=process.env.SEED_ADMIN_PASSWORD??'ChangeMe123!';const admin=await prisma.user.upsert({where:{email:'admin@cerdasceria.sch.id'},update:{name:'Administrator',username:'admin',passwordHash:await bcrypt.hash(adminPassword,12),status:'ACTIVE'},create:{name:'Administrator',email:'admin@cerdasceria.sch.id',username:'admin',passwordHash:await bcrypt.hash(adminPassword,12)}});await prisma.userRole.upsert({where:{userId_roleId:{userId:admin.id,roleId:adminRole.id}},update:{},create:{userId:admin.id,roleId:adminRole.id}});const kelasA=await prisma.classRoom.upsert({where:{name_academicYear:{name:'Kelompok A',academicYear:'2026/2027'}},update:{},create:{name:'Kelompok A',academicYear:'2026/2027'}}),kelasB=await prisma.classRoom.upsert({where:{name_academicYear:{name:'Kelompok B',academicYear:'2026/2027'}},update:{},create:{name:'Kelompok B',academicYear:'2026/2027'}});const parentA=await prisma.parent.upsert({where:{phone:'+628111111111'},update:{name:'Budi Santoso'},create:{name:'Budi Santoso',phone:'+628111111111',dateOfBirth:new Date('1985-04-12')}}),parentB=await prisma.parent.upsert({where:{phone:'+628122222222'},update:{name:'Siti Rahma'},create:{name:'Siti Rahma',phone:'+628122222222',dateOfBirth:new Date('1987-08-23')}});const studentA=await prisma.student.upsert({where:{nis:'26001'},update:{name:'Aisyah Putri',classRoomId:kelasB.id,customSppAmount:null},create:{nis:'26001',name:'Aisyah Putri',dateOfBirth:new Date('2020-02-10'),classRoomId:kelasB.id,parents:{create:{parentId:parentA.id,isPrimary:true,relation:'Ayah'}}}}),studentB=await prisma.student.upsert({where:{nis:'26002'},update:{name:'Rafa Pratama',classRoomId:kelasA.id,customSppAmount:150000},create:{nis:'26002',name:'Rafa Pratama',dateOfBirth:new Date('2020-07-18'),classRoomId:kelasA.id,customSppAmount:150000,parents:{create:{parentId:parentB.id,isPrimary:true,relation:'Ibu'}}}});await prisma.studentParent.upsert({where:{studentId_parentId:{studentId:studentA.id,parentId:parentA.id}},update:{},create:{studentId:studentA.id,parentId:parentA.id,isPrimary:true,relation:'Ayah'}});const billB=await prisma.bill.upsert({where:{code:'SPP-2026-09-26002'},update:{},create:{studentId:studentB.id,classRoomId:kelasA.id,createdById:admin.id,code:'SPP-2026-09-26002',title:'SPP September 2026',periodLabel:'September 2026',billingMonth:9,billingYear:2026,originalAmount:150000,amount:150000,status:'PAID'}});await prisma.bill.upsert({where:{code:'SPP-2026-09-26001'},update:{},create:{studentId:studentA.id,classRoomId:kelasB.id,createdById:admin.id,code:'SPP-2026-09-26001',title:'SPP September 2026',periodLabel:'September 2026',billingMonth:9,billingYear:2026,originalAmount:250000,amount:250000,status:'UNPAID'}});const existingPayment=await prisma.payment.findUnique({where:{paymentNumber:'SPP-2026-000001'}});if(!existingPayment){const seededPayment=await prisma.payment.create({data:{studentId:studentB.id,recordedById:admin.id,paymentNumber:'SPP-2026-000001',method:'TRANSFER',amount:150000,allocations:{create:{billId:billB.id,amount:150000}}}});await prisma.receipt.create({data:{receiptNumber:'SPP-2026-000001',paymentId:seededPayment.id,issuedById:admin.id}});await prisma.auditLog.create({data:{userId:admin.id,action:'SEED_CREATE_PAYMENT',entity:'Payment',entityId:seededPayment.id,afterData:{paymentNumber:seededPayment.paymentNumber,amount:150000}}})}console.log(`Seed selesai: ${PERMISSION_CATALOG.length} permission granular.`)}main().catch(e=>{console.error(e);process.exit(1)}).finally(()=>prisma.$disconnect())